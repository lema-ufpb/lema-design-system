# Spec: CtaCentered

> CTA centrado editorial — blockus #01-#08.

---

## Propósito

Wrapper de `Cta` `align center` com `max-w-2xl`.

---

## Localização

| Campo | Valor |
|---|---|
| Arquivo | `components/ds/cta-centered.tsx` |
| Tipo | `registry:block` (name: `ds-cta-centered`) |
| Categoria | CTA |
| Depende de | `Cta` |

---

## API — Props

Herda `CtaProps` sem `align` (fixo center).

---

## Stories

- [ ] `Default`
- [ ] `WithBadge`

---

## Checklist

- [x] Reuso `Cta` sem duplicação
